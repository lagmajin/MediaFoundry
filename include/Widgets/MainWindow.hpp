#pragma once

#include <QtWidgets/QMainWindow>


namespace MediaFoundry {

 class MediaFoundryMainWindow:public QMainWindow {
 private:

 public:
  explicit MediaFoundryMainWindow(QWidget* parent = nullptr);
  ~MediaFoundryMainWindow();
 };







}